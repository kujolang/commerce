# Operator runbook

Inspect by correlation ID: semantic operation, provider mapping, webhook receipt, job, dead letter, cursor, and drift finding. Query current provider state before retrying an unknown outcome. Replay only after fixing the failure cause, through an authorization hook, with an audit record and the original stable event/operation identity. Keep report-only reconciliation unless repair is explicitly approved. Provider-dashboard links are conveniences, not evidence of local projection state.
